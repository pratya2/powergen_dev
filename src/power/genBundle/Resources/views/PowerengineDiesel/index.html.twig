<h1>PowerengineDiesel list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Dieselid</th>
            <th>Id</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('powerenginediesel_show', { 'id': entity.id }) }}">{{ entity.dieselId }}</a></td>
            <td>{{ entity.id }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('powerenginediesel_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('powerenginediesel_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('powerenginediesel_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
