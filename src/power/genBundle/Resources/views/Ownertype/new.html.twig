<h1>Ownertype creation</h1>

<form action="{{ path('ownertype_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('ownertype') }}">
            Back to the list
        </a>
    </li>
</ul>
